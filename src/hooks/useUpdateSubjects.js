import db from "@database";
import { useEffect, useState } from "react";

const useUpdateSubjects = async () => {
  //   const [updateSubjects, setUpdateSubjects] = useState([]);
  //   const subjects = await db.getFaduSubjects();
  //   setUpdateSubjects(subjects);
  //   return {
  //     updateSubjects,
  //     setUpdateSubjects,
  //   };
};

export default useUpdateSubjects;
